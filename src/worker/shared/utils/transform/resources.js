export const resourceResponse = (resourceData) => ({
    allowedTileTypes: resourceData.allowedTileTypes,
    amount: resourceData.amount,
    attributes: resourceData.attributes,    
    balance: resourceData.balance,
    breakDown: resourceData.breakDown,
    cap: resourceData.cap,
    capMult: resourceData.capMult,
    consumption: resourceData.consumption,
    defaultCap: resourceData.defaultCap,
    earned: resourceData.earned,
    hasCap: resourceData.hasCap,
    id: resourceData.id,
    rarity: resourceData.rarity,    
    rawCap: resourceData.rawCap,
    searchableMeta: resourceData.searchableMeta,
    sellPrice: resourceData.sellPrice,
    spent: resourceData.spent,
    storageBreakdown: resourceData.storageBreakdown,
    breakdown: resourceData.breakdown,
    description: resourceData.description,
    name: resourceData.name,
    tags: resourceData.tags,
    targetEfficiency: resourceData.targetEfficiency,
    isPinned: resourceData.isPinned,
    income: resourceData.income,
    multiplier: resourceData.multiplier,
    consumption: resourceData.consumption
})